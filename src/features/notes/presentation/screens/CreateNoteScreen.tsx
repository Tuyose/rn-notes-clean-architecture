import React from 'react';
import { NoteEditorScreen } from './NoteEditorScreen';

/**
 * Create note route — delegates to the unified NoteEditorScreen.
 */
export function CreateNoteScreen() {
  return <NoteEditorScreen />;
}
